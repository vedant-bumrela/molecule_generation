# System Architecture - AI-Enabled Molecular Docking

## 🏛️ High-Level Architecture

```
┌────────────────────────────────────────────────────────────────────┐
│                         USER LAYER                                 │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐            │
│  │ Web Browser  │  │  CLI Tool    │  │ Python API   │            │
│  │ (Port 5000)  │  │  (dock.py)   │  │ (Import)     │            │
│  └──────┬───────┘  └──────┬───────┘  └──────┬───────┘            │
└─────────┼──────────────────┼──────────────────┼────────────────────┘
          │                  │                  │
          │ HTTP/REST        │ Direct Call      │ Import
          │                  │                  │
┌─────────▼──────────────────▼──────────────────▼────────────────────┐
│                      APPLICATION LAYER                             │
│  ┌────────────────────────────────────────────────────────────┐   │
│  │              Flask API Server (app.py)                     │   │
│  │  • Job submission & validation                             │   │
│  │  • Status tracking                                         │   │
│  │  • Result retrieval                                        │   │
│  │  • Static file serving                                     │   │
│  └────────────────┬───────────────────────────────────────────┘   │
│                   │                                                │
│  ┌────────────────▼───────────────────────────────────────────┐   │
│  │         Core Processing Modules                            │   │
│  │  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐    │   │
│  │  │ Preprocessing│  │   Docking    │  │  ML Models   │    │   │
│  │  │              │  │              │  │              │    │   │
│  │  │ • Protein    │  │ • Vina       │  │ • GNINA      │    │   │
│  │  │ • Ligand     │  │ • Scoring    │  │ • EquiBind   │    │   │
│  │  │ • Validation │  │ • Pose Gen   │  │ • DiffDock   │    │   │
│  │  └──────────────┘  └──────────────┘  └──────────────┘    │   │
│  └────────────────────────────────────────────────────────────┘   │
└────────────────────────┬───────────────────────────────────────────┘
                         │
                         │ Redis Protocol
                         │
┌────────────────────────▼───────────────────────────────────────────┐
│                      QUEUE LAYER                                   │
│  ┌────────────────────────────────────────────────────────────┐   │
│  │                  Redis (Port 6379)                         │   │
│  │  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐    │   │
│  │  │ docking_tasks│  │ batch_tasks  │  │  result:*    │    │   │
│  │  │   (Queue)    │  │   (Queue)    │  │   (Cache)    │    │   │
│  │  └──────────────┘  └──────────────┘  └──────────────┘    │   │
│  └────────────────────────────────────────────────────────────┘   │
└────────────────────────┬───────────────────────────────────────────┘
                         │
                         │ BLPOP (Blocking Pop)
                         │
┌────────────────────────▼───────────────────────────────────────────┐
│                      WORKER LAYER                                  │
│  ┌────────────────────────────────────────────────────────────┐   │
│  │              Worker Process (worker.py)                    │   │
│  │  • Poll Redis queues                                       │   │
│  │  • Execute docking pipeline                                │   │
│  │  • Store results back to Redis                             │   │
│  │  • Handle errors & retries                                 │   │
│  │                                                             │   │
│  │  [Can scale horizontally - multiple workers]               │   │
│  └────────────────────────────────────────────────────────────┘   │
└────────────────────────────────────────────────────────────────────┘
```

## 🔄 Request Flow Diagram

### Single Docking Job

```
┌─────────┐
│  User   │
└────┬────┘
     │ 1. Upload protein.pdb + ligand.sdf
     ▼
┌─────────────────┐
│   Flask API     │
│  /api/submit    │
└────┬────────────┘
     │ 2. Validate files
     │ 3. Generate job_id
     │ 4. Save files to disk
     ▼
┌─────────────────┐
│     Redis       │
│ RPUSH job_data  │
└────┬────────────┘
     │ 5. Job queued (status: pending)
     │
     │ 6. Worker polls queue
     ▼
┌─────────────────────────────────────┐
│           Worker Process            │
│                                     │
│  ┌─────────────────────────────┐   │
│  │ Step 1: Prepare Protein     │   │
│  │  • Fix structure            │   │
│  │  • Add hydrogens            │   │
│  │  • Assign charges           │   │
│  │  • Convert to PDBQT         │   │
│  └─────────────────────────────┘   │
│              ↓                      │
│  ┌─────────────────────────────┐   │
│  │ Step 2: Prepare Ligand      │   │
│  │  • Parse input              │   │
│  │  • Generate 3D coords       │   │
│  │  • Optimize geometry        │   │
│  │  • Convert to PDBQT         │   │
│  └─────────────────────────────┘   │
│              ↓                      │
│  ┌─────────────────────────────┐   │
│  │ Step 3: Run Docking         │   │
│  │  • Setup binding site       │   │
│  │  • Execute Vina             │   │
│  │  • Parse output             │   │
│  │  • Extract poses            │   │
│  └─────────────────────────────┘   │
│              ↓                      │
│  ┌─────────────────────────────┐   │
│  │ Step 4: Store Results       │   │
│  │  • Save to Redis            │   │
│  │  • Update status            │   │
│  └─────────────────────────────┘   │
└─────────────────────────────────────┘
     │ 7. Results stored (status: completed)
     ▼
┌─────────────────┐
│     Redis       │
│ SET result:id   │
└────┬────────────┘
     │ 8. User polls /api/status/<id>
     ▼
┌─────────────────┐
│   Flask API     │
│  Returns result │
└────┬────────────┘
     │ 9. Display in browser
     ▼
┌─────────┐
│  User   │
└─────────┘
```

## 🗂️ Module Dependencies

```
dock.py (CLI)
    │
    ├─→ src.preprocessing.ProteinPreparation
    │       │
    │       ├─→ BioPython (structure parsing)
    │       ├─→ PDBFixer (structure fixing)
    │       ├─→ Open Babel (hydrogen addition)
    │       └─→ Meeko (charge assignment)
    │
    ├─→ src.preprocessing.LigandPreparation
    │       │
    │       ├─→ RDKit (SMILES → 3D)
    │       ├─→ Open Babel (format conversion)
    │       └─→ Meeko (PDBQT conversion)
    │
    └─→ src.docking.VinaDocking
            │
            ├─→ AutoDock Vina (docking engine)
            └─→ Open Babel (pose extraction)

src.api.app (Flask API)
    │
    ├─→ Flask (web framework)
    ├─→ Flask-CORS (cross-origin requests)
    ├─→ Redis (job queue)
    │
    └─→ src.api.worker
            │
            ├─→ Redis (queue polling)
            ├─→ src.preprocessing.*
            ├─→ src.docking.*
            └─→ src.ml.*

src.ml.* (ML Models)
    │
    ├─→ PyTorch (deep learning)
    ├─→ PyTorch Geometric (graph networks)
    └─→ Model-specific dependencies
```

## 🐳 Docker Architecture

```
┌─────────────────────────────────────────────────────────┐
│              Docker Compose Network                     │
│                                                         │
│  ┌──────────────────┐  ┌──────────────────┐           │
│  │   docking-app    │  │  docking-worker  │           │
│  │   (Flask API)    │  │   (Worker)       │           │
│  │   Port: 5000     │  │   No ports       │           │
│  │   ┌──────────┐   │  │   ┌──────────┐   │           │
│  │   │ Python   │   │  │   │ Python   │   │           │
│  │   │ Flask    │   │  │   │ Worker   │   │           │
│  │   │ Vina     │   │  │   │ Vina     │   │           │
│  │   │ RDKit    │   │  │   │ RDKit    │   │           │
│  │   └──────────┘   │  │   └──────────┘   │           │
│  └────────┬─────────┘  └────────┬─────────┘           │
│           │                     │                      │
│           │ Redis Protocol      │                      │
│           └──────────┬──────────┘                      │
│                      │                                 │
│            ┌─────────▼─────────┐                       │
│            │  docking-redis    │                       │
│            │  (Redis Server)   │                       │
│            │  Port: 6379       │                       │
│            │  ┌─────────────┐  │                       │
│            │  │   Redis     │  │                       │
│            │  │   Queue     │  │                       │
│            │  └─────────────┘  │                       │
│            └───────────────────┘                       │
│                                                         │
│  Shared Volumes:                                       │
│  • shared-uploads (persistent)                         │
│  • redis-data (persistent)                             │
└─────────────────────────────────────────────────────────┘
```

## 📦 Data Storage

```
File System Layout:

/app/shared/
├── uploads/
│   └── <job_id>/
│       ├── protein.pdb          (user upload)
│       └── ligand.sdf           (user upload)
│
└── results/
    └── <job_id>/
        ├── protein_prepared.pdbqt
        ├── ligand_prepared.pdbqt
        ├── docked.pdbqt         (all poses)
        ├── docked.json          (scores)
        ├── top_pose.pdb         (best pose)
        └── poses/
            ├── pose_1.pdb
            ├── pose_2.pdb
            └── ...

Redis Storage:

Key Pattern                    Type      TTL      Purpose
─────────────────────────────────────────────────────────────
docking_tasks                  List      -        Job queue
batch_tasks                    List      -        Batch queue
result:<job_id>                String    7 days   Job results
progress:<job_id>              String    7 days   Progress info
```

## 🔌 API Endpoints

```
┌─────────────────────────────────────────────────────────┐
│                    REST API Endpoints                   │
├─────────────────────────────────────────────────────────┤
│                                                         │
│  GET  /                                                 │
│       → Serve web interface (index.html)                │
│                                                         │
│  GET  /api/health                                       │
│       → Health check                                    │
│       ← {"status": "ok"}                                │
│                                                         │
│  POST /api/submit                                       │
│       ← protein (file), ligand (file), params           │
│       → {"job_id": "...", "status": "pending"}          │
│                                                         │
│  GET  /api/status/<job_id>                              │
│       → {"job_id": "...", "status": "...", ...}         │
│                                                         │
│  GET  /api/jobs                                         │
│       → {"jobs": [{...}, {...}]}                        │
│                                                         │
│  GET  /api/result/<job_id>/pdbqt                        │
│       → Download docked.pdbqt                           │
│                                                         │
│  GET  /api/result/<job_id>/pdb                          │
│       → Download top_pose.pdb                           │
│                                                         │
│  POST /api/submit/batch                                 │
│       ← protein, library (CSV/SDF), params              │
│       → {"job_id": "...", "status": "pending"}          │
│                                                         │
│  GET  /api/batch/results/<job_id>                       │
│       → {"results": [{...}, {...}]}                     │
│                                                         │
└─────────────────────────────────────────────────────────┘
```

## 🔐 Security Model

```
┌─────────────────────────────────────────────────────────┐
│                   Security Layers                       │
├─────────────────────────────────────────────────────────┤
│                                                         │
│  1. Input Validation                                    │
│     • File extension whitelist                          │
│     • File size limits (50MB)                           │
│     • MIME type checking                                │
│                                                         │
│  2. File Isolation                                      │
│     • Each job in separate directory                    │
│     • UUID-based job IDs                                │
│     • No path traversal                                 │
│                                                         │
│  3. Resource Limits                                     │
│     • Max upload size                                   │
│     • Result TTL (7 days)                               │
│     • Queue size limits                                 │
│                                                         │
│  4. CORS Policy                                         │
│     • Currently: Allow all (development)                │
│     • Production: Restrict origins                      │
│                                                         │
│  5. Container Isolation                                 │
│     • Docker network isolation                          │
│     • No privileged containers                          │
│     • Volume mounts read-only where possible            │
│                                                         │
│  ⚠️  Missing (Add for Production):                      │
│     • User authentication                               │
│     • API rate limiting                                 │
│     • HTTPS/TLS                                         │
│     • Input sanitization                                │
│     • Audit logging                                     │
│                                                         │
└─────────────────────────────────────────────────────────┘
```

## 📊 Scalability Considerations

```
Horizontal Scaling:

┌─────────────┐
│ Load        │
│ Balancer    │
└──────┬──────┘
       │
   ┌───┴───┬───────┬───────┐
   │       │       │       │
┌──▼──┐ ┌──▼──┐ ┌──▼──┐ ┌──▼──┐
│ API │ │ API │ │ API │ │ API │
│  1  │ │  2  │ │  3  │ │  4  │
└──┬──┘ └──┬──┘ └──┬──┘ └──┬──┘
   │       │       │       │
   └───┬───┴───────┴───────┘
       │
   ┌───▼────┐
   │ Redis  │
   │ Cluster│
   └───┬────┘
       │
   ┌───┴───┬───────┬───────┐
   │       │       │       │
┌──▼──┐ ┌──▼──┐ ┌──▼──┐ ┌──▼──┐
│Work │ │Work │ │Work │ │Work │
│  1  │ │  2  │ │  3  │ │  4  │
└─────┘ └─────┘ └─────┘ └─────┘

Bottlenecks:
• Redis (single point) → Use Redis Cluster
• File I/O → Use distributed storage (S3, NFS)
• Vina CPU-bound → Add more workers
```

## 🔄 State Machine

```
Job Lifecycle:

    [Submit]
       │
       ▼
  ┌─────────┐
  │ PENDING │ ←──────┐
  └────┬────┘        │
       │             │ (Retry)
       ▼             │
  ┌─────────┐        │
  │ RUNNING │ ───────┤
  └────┬────┘        │
       │             │
   ┌───┴───┐         │
   │       │         │
   ▼       ▼         │
┌──────┐ ┌────────┐  │
│ DONE │ │ FAILED │ ─┘
└──────┘ └────────┘

States:
• PENDING   - In queue, waiting for worker
• RUNNING   - Being processed by worker
• COMPLETED - Successfully finished
• FAILED    - Error occurred (with error message)
```

---

**This architecture supports**:
- ✅ Asynchronous job processing
- ✅ Horizontal scaling
- ✅ Fault tolerance (Redis persistence)
- ✅ Multiple docking methods
- ✅ Batch processing
- ✅ Real-time status updates
